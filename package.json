{
  "name": "ftp-uploader",
  "version": "1.0.0",
  "description": "Remote server file uploader with ftp",
  "main": "src/index.js",
  "scripts": {
    "watch": "tsc -w",
    "start": "tsc && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "msebaktir",
  "license": "ISC",
  "devDependencies": {
    "ts-loader": "^9.3.0",
    "typescript": "^4.6.4"
  },
  "dependencies": {
    "@types/ftp": "^0.3.33",
    "basic-ftp": "^4.6.6"
  }
}
